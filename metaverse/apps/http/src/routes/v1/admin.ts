import { Router } from "express";

export const adminRouter = Router();

adminRouter.post("/element", (req, req) => {});
adminRouter.put("/element/:elementId", (req, req) => {});
adminRouter.get("/avatar", (req, req) => {});

// /api/v1/space
adminRouter.get("/map", (req, req) => {});
